generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// ── Admin / User ─────────────────────────────────────────────────────────────
model User {
  id           String   @id @default(uuid())
  email        String   @unique
  password     String
  isVerified   Boolean  @default(false)
  refreshToken String?

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("users")
}

// ── Registered Vehicle (authorised sticker holders) ──────────────────────────
model Vehicle {
  id          String   @id @default(uuid())
  name        String
  fathersName String
  dept        String
  dateOfIssue DateTime
  vehicleType String              // "2W" | "4W" | "Heavy" | "Electric"
  stickerNo   String   @unique
  vehicleNo   String   @unique
  mobileNo    String

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  logs EntryExitLog[]

  @@map("vehicles")
}

// ── Campus Camera ─────────────────────────────────────────────────────────────
model Camera {
  id             String @id @default(uuid())
  lat            Float
  long           Float
  cameraType     String  // "ENTRY" | "EXIT" | "BOTH" | "INTERIOR"
  cameraLocation String

  createdAt DateTime @default(now())

  logs EntryExitLog[]

  @@map("cameras")
}

// ── Entry / Exit / Sighting Log ───────────────────────────────────────────────
// logType:
//   ENTRY    – vehicle entered campus (gate camera, no prior open log)
//   EXIT     – vehicle exited campus  (gate camera, closes an ENTRY log)
//   SIGHTING – vehicle spotted at interior/area camera (no entry/exit impact)


model EntryExitLog {
  id       String @id @default(uuid())
  cameraId String
  camera   Camera @relation(fields: [cameraId], references: [id])

  vehicleNo String
  vehicle   Vehicle? @relation(fields: [vehicleNo], references: [vehicleNo])

  logType         String    @default("ENTRY")  // "ENTRY" | "EXIT" | "SIGHTING"
  confidence      Float?                        // confidence score from hardware JSON
  rawPlate        String                        // plate string as received from hardware
  entryTime       DateTime  @default(now())
  exitTime        DateTime?                     // null unless EXIT
  vehicleDuration Int?                          // seconds (gate cameras only)
  isAuthorized    Boolean   @default(false)     // true if vehicleNo in Vehicle table
  vehicleCategory String    @default("REGISTERED") 
  // "REGISTERED" | "UNVERIFIED" (cab/auto/taxi/delivery – 30-min window)

  createdAt DateTime @default(now())

  @@map("entry_exit_logs")
}
